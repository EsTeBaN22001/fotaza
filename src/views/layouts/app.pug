doctype html
html
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Fotaza App

    link(
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css",
      rel="stylesheet"
    )

  body.bg-light

    // 🔝 NAVBAR PRINCIPAL
    nav.navbar.navbar-expand-lg.navbar-dark.bg-dark.shadow-sm
      .container

        a.navbar-brand(href="/home") Fotaza

        button.navbar-toggler(
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarApp"
        )
          span.navbar-toggler-icon

        .collapse.navbar-collapse#navbarApp

          // izquierda (links principales)
          ul.navbar-nav.me-auto
            li.nav-item
              a.nav-link(href="/home") Inicio
            li.nav-item
              a.nav-link(href="/posts/create") Crear Post

          // derecha (usuario)
          ul.navbar-nav.ms-auto
            if user
              li.nav-item.dropdown
                a.nav-link.dropdown-toggle(
                  href="#"
                  role="button"
                  data-bs-toggle="dropdown"
                ) #{user.username}

                ul.dropdown-menu.dropdown-menu-end
                  li
                    a.dropdown-item(href="/profile/my-posts") Perfil
                  li
                    hr.dropdown-divider
                  li
                    a.dropdown-item.text-danger(href="/auth/logout") Cerrar sesión

    // 📢 MENSAJES GLOBALES
    .container.mt-3
      if success
        .alert.alert-success.text-center= success

      if errors && errors.length
        .alert.alert-danger
          each err in errors
            div= err.message

    // 📦 CONTENIDO PRINCIPAL
    .container.mt-4
      block content

    // footer simple (opcional)
    footer.text-center.text-muted.mt-5.mb-3
      small © 2026 Fotaza

    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js")